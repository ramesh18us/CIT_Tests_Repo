import org.junit.Test;

import java.util.Random;


public class CI_Test {
        Random ran = new Random();
        int range = 9;
        int randomNumber;
        int number;

        @Test
        public void mfe_lrt_testone() {

            randomNumber = ran.nextInt(range) + 1;
            passfail.dummycode(randomNumber);
        }


        @Test
        public void mfe_lrt_testtwo() {

            randomNumber = ran.nextInt(range) + 1;
            passfail.dummycode(randomNumber);
        }

        @Test
        public void mfe_lrt_testthree() {

            randomNumber = ran.nextInt(range) + 1;
            passfail.dummycode(randomNumber);
        }


        @Test
        public void mfe_ste_testone() {

            randomNumber = ran.nextInt(range) + 1;
            passfail.dummycode(randomNumber);
        }


        @Test
        public void mfe_ste_testtwo() {

            randomNumber = ran.nextInt(range) + 1;
            passfail.dummycode(randomNumber);
        }

        @Test
        public void mfe_ste_testthree() {

            randomNumber = ran.nextInt(range) + 1;
            passfail.dummycode(randomNumber);
        }

        @Test
        public void mfe_ste_testfour() {

            randomNumber = ran.nextInt(range) + 1;
            passfail.dummycode(randomNumber);
        }

        @Test
        public void mfe_sct_testone() {

            randomNumber = ran.nextInt(range) + 1;
            passfail.dummycode(randomNumber);
        }


        @Test
        public void mfe_sct_testtwo() {

            randomNumber = ran.nextInt(range) + 1;
            passfail.dummycode(randomNumber);
        }

        @Test
        public void mfe_sct_testthree() {

            randomNumber = ran.nextInt(range) + 1;
            passfail.dummycode(randomNumber);
        }


    @Test
        public void mks_fde_testone() {

            randomNumber = ran.nextInt(range) + 1;
            passfail.dummycode(randomNumber);
        }


        @Test
        public void mks_fde_testtwo() {

            randomNumber = ran.nextInt(range) + 1;
            passfail.dummycode(randomNumber);
        }

        @Test
        public void mks_fde_testthree() {

            randomNumber = ran.nextInt(range) + 1;
            passfail.dummycode(randomNumber);
        }

        @Test
        public void mks_fde_testfour() {

            randomNumber = ran.nextInt(range) + 1;
            passfail.dummycode(randomNumber);
        }

        @Test
        public void mks_fde_testfive() {

            randomNumber = ran.nextInt(range) + 1;
            passfail.dummycode(randomNumber);
        }
        @Test
        public void mks_lit_testone() {

            randomNumber = ran.nextInt(range) + 1;
            passfail.dummycode(randomNumber);
        }

        @Test
        public void mks_lit_testtwo() {

            randomNumber = ran.nextInt(range) + 1;
            passfail.dummycode(randomNumber);
        }
        
        @Test
        public void mks_cdi_testone() {

            randomNumber = ran.nextInt(range) + 1;
            passfail.dummycode(randomNumber);
        }

        @Test
        public void mks_cdi_testtwo() {

            randomNumber = ran.nextInt(range) + 1;
            passfail.dummycode(randomNumber);
        }

        @Test
        public void gtw_ord_testone() {

            randomNumber = ran.nextInt(range) + 1;
            passfail.dummycode(randomNumber);
        }


        @Test
        public void gtw_ord_testtwo() {

            randomNumber = ran.nextInt(range) + 1;
            passfail.dummycode(randomNumber);
        }

        @Test
        public void gtw_ord_testthree() {

            randomNumber = ran.nextInt(range) + 1;
            passfail.dummycode(randomNumber);
        }

        @Test
        public void gtw_udb_testone() {

            randomNumber = ran.nextInt(range) + 1;
            passfail.dummycode(randomNumber);
        }


        @Test
        public void gtw_udb_testtwo() {

            randomNumber = ran.nextInt(range) + 1;
            passfail.dummycode(randomNumber);
        }

        @Test
        public void gtw_udb_testthree() {

            randomNumber = ran.nextInt(range) + 1;
            passfail.dummycode(randomNumber);
        }

        @Test
        public void gtw_udb_testfour() {

            randomNumber = ran.nextInt(range) + 1;
            passfail.dummycode(randomNumber);
        }


}

